class GlobalConstants {
  // The meta data of a file with name "FILENAME" is stored in the file "FILENAME.meta" (same directory as original file).
  static final String MetaDataFileSuffix = '.meta';

  static final int FileNameSize = 8;
  static final int FileSize = 10;
  static final int maxClients = 1000;
}
